<h2>Modalidades de Entrega</h2>


<div>
  <% if @delivery_modalities.any? %>
  
    <dl>
      <% @delivery_modalities.each do |m|%>
        <dt><strong><%= DeliveryModality.human_attribute_name(:mod_name) %>:</strong> <%= link_to m.mod_name, m%></dt>
        <dd><strong><%= DeliveryModality.human_attribute_name(:mod_price) %>:</strong> <%= number_to_currency(m.mod_price) %></dd>
      <%end%>
    </dl> 
  
  <% else %>
    <p>Não existem modalidades cadastradas.</p>
  <% end %>
</div>
<div>
<% if current_user.user_access == 'admin_user' %>
<%= link_to 'Cadastrar Modalidade', new_delivery_modality_path %>

<% end %>
</div>